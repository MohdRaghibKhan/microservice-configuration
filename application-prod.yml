eureka:
  instance:
    prefer-ip-address: true
spring:
  application:
    name: CONFIG-PROD
  profiles:
    active: prod
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
      
